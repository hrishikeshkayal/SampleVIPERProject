package com.codeeden.sampleviperproject.network;

import retrofit2.Call;
import retrofit2.http.Body;
import retrofit2.http.POST;

;

/**
 * Created by Hrishi on 5/14/2018.
 */
public interface WSSCApService {

   /* @POST("login")
    Call<LoginResponse> loginService(@Body LoginRequest loginRequest);*/

    /*
    @Multipart
    @POST("api/addchrosity")
    Call<ResponseChromatacity> addchrosityService(@Part("user_id") RequestBody user_id,
                                                  @Part("chromosityX") RequestBody chromosityX,
                                                  @Part("chromosityY") RequestBody chromosityY,
                                                  @Part("luminance") RequestBody luminance,
                                                  @Part("color") RequestBody color,
                                                  @Part("status") RequestBody status,
                                                  @Part("latitude") RequestBody lat,
                                                  @Part("longitude") RequestBody longi,
                                                  @Part("address") RequestBody address,
                                                  @Part MultipartBody.Part image);
    */

}